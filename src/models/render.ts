export interface RenderAttrs {
  url: string;
  email: string;
  tags: string[];
  capacity: number;
  serviceId: string;
  authToken: string;
  imageName: string;
  isActive: boolean;
  isLocked: boolean;
}
